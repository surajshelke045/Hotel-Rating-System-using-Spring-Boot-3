

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761
  

spring:
  config:
    import: optional:application.yml
  cloud:
    config:
      import-check:
        enabled: false
